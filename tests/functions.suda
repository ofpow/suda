fn fib(n)
    if (n < 2)
        return 1
    ;

    let n1 = (n - 1)
    let n2 = (n - 2)

    return (fib(n1) + fib(n2))
;

println fib(10)

fn add(a, b)
    println(a + b)
    return "E"
;

add(34, 35)