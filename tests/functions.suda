fn fib(n)
    if (n < 2)
        return 1
    ;
    return ((fib(n - 1)) + (fib(n - 2)))
;

println fib(10)

fn add(a, b)
    return (a + b)
;

println add(34, 17 + 18)

fn foo(x)
    println x@3
;

foo("hello")
